# Architecture

System shape: services, modules, data flow, external dependencies, boundaries.

Keep entries short. Prefer a diagram-as-text (ascii or mermaid) + 3 bullets over paragraphs.

<!-- Append entries below. Each starts with `## <title>`. -->
